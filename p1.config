## section for logstash to recive data from QRadar send to kafka
input { 
  stdin { }
 }

filter {}

output { 
    stdout {}
    kafka {
      it => "test_topic"
      topic_id => "test_topic"
      codec => "json"
      bootstrap_servers => "localhost:9092"
    }
}
